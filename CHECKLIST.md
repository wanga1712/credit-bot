# Чеклист для переноса на Linux

## ✅ Проверка перед коммитом

- [x] Все исходные файлы проекта на месте
- [x] `.gitignore` настроен правильно (игнорирует venv, __pycache__, .env)
- [x] `requirements.txt` содержит все зависимости
- [x] `README.md` содержит инструкции по установке
- [x] `DEPLOY_LINUX.md` создан с инструкциями для Linux
- [x] Git репозиторий инициализирован
- [x] Все файлы добавлены в staging

## 📋 Файлы, которые НЕ должны попасть в git

- ✅ `.env` - файл с токеном (уже в .gitignore)
- ✅ `venv/` - виртуальное окружение (уже в .gitignore)
- ✅ `__pycache__/` - кэш Python (уже в .gitignore)
- ✅ `.pytest_cache/` - кэш pytest (уже в .gitignore)

## 📝 Что нужно сделать на Linux сервере

1. **Клонировать репозиторий:**
   ```bash
   git clone <url_вашего_репозитория>
   cd pythonProject93
   ```

2. **Создать виртуальное окружение:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

3. **Установить зависимости:**
   ```bash
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

4. **Создать файл .env:**
   ```bash
   # Создайте файл .env в корне проекта
   echo "TELEGRAM_BOT_TOKEN=ваш_токен_здесь" > .env
   # Или отредактируйте вручную
   nano .env
   ```

5. **Запустить бота:**
   ```bash
   python main.py
   ```

## 🔍 Проверка перед отправкой на сервер

Выполните следующие команды для проверки:

```bash
# Проверить статус git
git status

# Проверить, что .env не в git
git ls-files | grep -E "\.env$"

# Проверить, что venv не в git
git ls-files | grep -E "venv/"

# Просмотреть все файлы, которые будут закоммичены
git diff --cached --name-only
```

## 📦 Создание первого коммита

```bash
git commit -m "Initial commit: Credit Bot project"
```

## 🚀 Отправка на сервер

После создания коммита, отправьте код в удаленный репозиторий:

```bash
# Добавьте remote (если еще не добавлен)
git remote add origin <url_вашего_репозитория>

# Отправьте код
git push -u origin master
```

## ⚠️ Важно

- **НЕ коммитьте файл `.env`** с реальным токеном
- **НЕ коммитьте `venv/`** - виртуальное окружение создается на сервере
- На Linux используйте Python 3.11 или выше
- Убедитесь, что на сервере установлен `pip` и `python3`

